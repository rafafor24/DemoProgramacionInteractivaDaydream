using UnityEngine;
using UnityEngine.UI;

public class ValueX : MonoBehaviour
{
    public GameObject gameObje;
    public Text text;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
